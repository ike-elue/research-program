- rate the entries based on point system
- make it an applet
- Say somewhere which keyword detected what
    - maybe put in hashmap of list where the keys are lists allowing for easier formatting
- Add replicate search button
    - saves details of scraper to separate file accordingly
- Have "are you sure (JOptionPane)" in the reset index
- Make the program able to open up the saved file and show it in a special format
    - has each website as a button that when clicked shows three buttons: info gathered, stats, replicate file search
        - each button name speaks for itself